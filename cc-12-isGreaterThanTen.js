// Given a number, return whether the number is greater than 10

function isGreaterThanTen(num) {
  return num > 10 ? true : false;
}

//check if it's less than 30

function isLessThanThirty(num) {
  return num < 30 ? true : false;
}

// given a number, return whether or not the num is 10

function isTen(num) {
  return num === 10 ? true : false;
}
//given 2 numbers, return whether or not the second is less than the first

function isLessThan(num1, num2) {
  return num2 < num1 ? true : false;
}
