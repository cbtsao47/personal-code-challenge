// write a function called isOldEnoughToDrive'
// Given a number, in this case an AudioProcessingEvent, the function returns whether a person of the given age is old enough to drive in US
// Notes:
// legal driving age is 16

function isOldEnoughToDrive(age) {
  return age >= 16 ? true : false;
}
